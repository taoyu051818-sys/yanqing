# 验收手册

## 自动验收

```bash
pnpm install --frozen-lockfile
pnpm verify:full
```

基线数字会随业务闭环增加，验收时以命令实际输出为准。`pnpm verify` 覆盖 shared 领域规则、API 单元/E2E、NestJS 构建、小程序测试/类型检查/微信构建；`verify:full` 额外只检查上游 Next.js 视觉原型仍可构建，不能把该原型计为已联调的 Web 管理后台。

若只验收 API 与小程序，可执行：

```bash
pnpm test:shared
pnpm test:api
pnpm --dir apps/api test:e2e
pnpm build:api
pnpm typecheck:miniapp
pnpm build:miniapp
```

微信开发者工具不能由上述命令代替，必须使用构建产物导入并完成模拟器、预览真机和体验版验收，步骤见 [docs/wechat-devtools.md](wechat-devtools.md)。

## 数据库验收

```bash
docker compose up -d postgres
pnpm --dir apps/api db:deploy
pnpm --dir apps/api db:seed
pnpm dev:api
```

访问 `http://127.0.0.1:3200/docs`，依次执行开发登录、`GET /auth/me`、`GET /venues/availability`、创建订场订单、支付和签到。重复使用同一支付幂等键应返回同一支付记录；并发预订同一场地时段只能成功一次。

## 关键场景

1. 会员查看 20 片场地并下单；支付后从 `HELD` 变为 `CONFIRMED`，扫码后为 `CHECKED_IN`。同一场地/时段并发下单只能成功一次。
2. 五账户分别充值/扣减，确认任一流水不会改变其他账户；联盟商户端不存在会员余额支付入口。
3. 绑定直接推荐人后再次改绑必须失败；推荐奖励只出现在直接推荐人账户，不产生上级奖励。
4. 球局报名支付后签到，满员后新会员进入 FIFO 候补；退款释放席位时最早候补自动生成待支付订单；主理人结束球局时按实际签到人数计算奖励；退款观察期内不提前发放，财务批处理可幂等补发。
5. 赛事少于 24 人签到不能开赛；完成五轮前不能完赛；固定双打、容量 48 人、让分起始分和 21 分封顶正确；纠错后排名重算并留审计。
6. 赛事草稿必须经过显式发布才开放报名；重复发布不重复写审计，已开始/已完成/已取消赛事不能再次发布。
7. 课包支付只增加预收余额；教练提交消课建议后由管理员确认才确认收入。对任意金额验证场馆合同收入为 20%，场地费和场馆应付款为 0。
8. 同一联盟券第一次核销成功，第二次失败且生成高风险事件；商户只能核销自己的券；结算支持提交、商户确认、异议（必填原因）和财务结算。
9. 商品支付后库存原子扣减；库存不足支付失败；整单退款后库存回补并产生反向流水。
10. 修改普通参数生成新生效版本，历史订单快照不变；培训 20% 和零场地费参数不能改成其他值。
11. 财务导出订单/会员/培训/联盟/库存/审计 Excel，确认导出动作进入审计日志。
12. 使用内部角色调用 `GET /work-items`，确认退款、待消课、待录比分、待结算、低库存和待履约事项按责任角色返回；会员调用时返回空列表。
13. 退款申请人与审批人不能是同一账号；真实微信退款在 `REFUND.SUCCESS` 通知前保持处理中，通知重放不重复冲正充值账户。
14. 前台、教练、赛事、商户、财务之间按对象和数据范围交接；越权、重复请求和非法状态动作均返回 4xx 并留下审计/风险记录。

## 微信验收

当前本地开发/预览构建使用测试 AppID `wxa457599ec4c27ad1`，构建产物应满足 `project.config.json.compileType = "miniprogram"`。在微信开发者工具导入 `apps/miniapp/dist/dev/mp-weixin`（开发）或 `apps/miniapp/dist/build/mp-weixin`（压缩构建/预览），不要导入源码目录。该测试号不支持上传；体验版需更换为支持上传且已授权的 AppID 后重新构建。先在 `VITE_DATA_MODE=mock` 下完成页面和角色旅程，再在 `VITE_DATA_MODE=remote` 下验证 API 持久化、权限和幂等。

在开发者工具和至少一台已加入体验成员的真机上逐项验证：微信登录、request 合法域名、扫码、支付/退款回调联调、弱网重试、分享返回、前后台切换、拒绝权限、安全区适配和不同屏幕尺寸。`urlCheck=false`、模拟支付、开发登录和本地 mock 只能作为开发演示，不能计为生产通过；真机不能访问电脑的 `127.0.0.1`，需使用 HTTPS 测试域名或受控局域网代理。生产 AppID、商户号、Secret、证书和 HTTPS 域名属于部署方资产，需在上线环境完成最终验收。

每条旅程记录角色、设备、AppID/版本、数据模式、订单/流水/风险事件 ID、前后状态、截图/日志和缺陷编号；完整记录模板见 [docs/wechat-devtools.md](wechat-devtools.md)。
