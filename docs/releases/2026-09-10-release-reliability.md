# 2026-09-10 发布、数据库就绪与恢复验收

## 已部署版本

- API 代码提交：`8d00aa1e64b59d34e6e75b28db30ce9a4d677a62`。
- [该提交 GitHub 全套检查通过](https://github.com/taoyu051818-sys/yanqing/actions/runs/34433065988)。本记录是后续文档提交，不改变部署代码编号。
- 线上目录：`/home/ubuntu/yanqing-domain/releases/api-8d00aa1e64b5`；原版本 `api-20260909-core-3aeddcb` 保留用于应用回滚。
- 发布包 SHA-256：`5eb25226363b71e30bfe1d4ed0b3ef433e9ebff6beffec40ea1f255601bafa8a`。
- 本机与公网 `/api/v1/health/ready` 均返回该完整提交、数据库 `ok`。服务正常运行，验收时自动重启次数为 0。
- 42 个迁移逐一校验一致；没有数据库迁移。未更改 PC 静态版本和业务环境配置；运行环境仍为 `staging`，支付通道为 `wechat`，开发登录为 `false`。

## 工程验证

GitHub 同一提交通过：后端单元 870 项、独立 PostgreSQL 核心矩阵 287 项、小程序 357 项、共享包 13 项、HTTP e2e 14 项、运维脚本 7 项、PC 1 项，以及 lint/API/PC/小程序构建。

单元阶段显示的 287 项数据库跳过用例，在后续独立数据库矩阵中实际执行；不是把跳过记成通过。

运维测试覆盖成功切换、就绪失败自动回退、手动回退、备份损坏或源版本变化拒绝激活，以及回退本身失败时不得误报成功。这部分系统服务操作使用隔离文件与测试替身，未在生产注入错误版本。

## 服务器备份与故障演练

准备阶段在 PostgreSQL 16 服务器创建实际备份并恢复到随机命名的隔离库，使用本次编译产物完成 10 项检查：启动、开发/匿名登录关闭、跨会员隔离、余额支付及重复支付、双方订单查询、退款及重复退款、取消释放场地、取消后拒付、数据库断连时 readiness 503/liveness 200、连接恢复后 readiness 200。

- 发布前备份：`/home/ubuntu/yanqing-domain/backups/verified/20260910T032535494Z-6a9d9adf/database.dump`，权限 `0600`，实际恢复成功。
- SHA-256：`e99fc7d7d4807fed35d519b959eda2e33d81722764fb2e8a51a62a187f9ed727`。
- 隔离库已删除，未向线上业务库写入验收订单或调用真实收付款。
- 发布证据目录：`/home/ubuntu/yanqing-domain/ops/receipts/8d00aa1e64b59d34e6e75b28db30ce9a4d677a62/`。
- 新的定时备份服务已手动执行一次，并再次完成备份恢复验证；后续成功记录为 `backups/verified/latest-verified.json`。

线上只读验收通过：管理订单、会员订单、球局、赛事、老板摘要和缓存简报；普通会员访问管理订单为 403，匿名订单和开发登录为 401。未发现启动后的 ERROR/未捕获异常标记。

## 运维与小程序

- `yanqing-backup.timer`：服务器时间每日 03:30 加最多 5 分钟延迟，备份后实际恢复验证；与发布互斥。
- `yanqing-health.timer`：每分钟检查本机/公网数据库就绪、实际提交、备份新鲜度和磁盘空间；记录 `ops/health.json` 与 journal。
- 微信开发者工具已成功生成本轮远端模式预览：AppID `wx25610460bc96894b`，主包 819013 字节，总包 1767981 字节。预览成功不等于真机业务验收；没有提交正式审核或发布微信客户端。
- 唯一当前部署、回滚和备份操作入口为 [部署与恢复手册](../deployment.md)。

## 尚未完成的运营验收

- 异地加密备份、独立外部探活及通知投递，仍需提供接入服务和接收渠道。当前备份与监测同机，不能覆盖整机磁盘损坏或整机失联。
- 会员、前台和好友真机验收，尤其真实微信支付/退款、代订后会员付款、分享直达及报名支付，仍须实际操作确认。本轮微信相关自动化使用测试替身。
