# 线上 H5 调试入口下线

执行时间：2026-09-05 21:18，北京时间。复核时间：21:20。

按用户要求，后续以微信小程序进行调试。旧 H5 入口已下线，不卸载共用 Nginx，也不影响同机其他站点。

- `https://yutechhn.cn/badminton`、`/badminton/` 及其资源路径统一返回410和小程序使用提示，不再代理到 H5。
- 移出专用 H5 的 Nginx 启用配置，确认本机38080端口不再监听。
- API 仍运行原版本，进程 PID 未改变；API 和主站 Nginx 配置摘要前后一致。
- 公开 API 健康检查200，未认证的头像上传接口401，主站200，旧 H5 入口410。
- 未运行数据库迁移、seed、角色授予或业务写操作；上传文件、支付配置和回调路由没有改动。本次没有发起真实支付或重新验收微信回调。

本次是可恢复下线，不是永久清空资源文件。保留已构建网页、历史 release、源代码和本地 H5 调试能力，以便回退及后续自动化测试。移走的启用配置和原入口配置位于远端：

`/home/ubuntu/yanqing-domain/backups/h5-retire-20260905-x6G7E4`

回退时恢复该目录中的原入口配置及启用链接，先 `nginx -t`，再 reload；不得重启或重置数据库。Nginx 其他站点未变更。

旧网页分享链接不再可用。小程序原生分享卡片使用包内图片与小程序页面路径，不依赖已下线的 H5 入口。用户已缓存的网页代码可能暂存于其浏览器中，下线网页不等于撤销 API 会话。

目前没有独立 PC 管理后台。小程序经营工作台保留；[后续 PC 管理后台方案](../admin-console-plan.md)明确两端分工、真实管理员引导和安全登录。线上当前仍允许开发登录，该策略没有因网页下线而自动关闭，须在真实管理通道建立后单独收口。
