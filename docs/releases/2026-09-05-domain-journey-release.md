# 用户旅程收口：域名服务器发布记录

发布时间：2026-09-05 20:49，北京时间。复核时间：20:53。

## 发布结论

- GitHub main 已推送：应用代码 `b3873cd00903fb3bf6a644dfbab7d3932ee16667`，发布检查脚本 `b0e56f648fe75136cadd567ce83adaf8149f24b1`。
- [H5 网页](https://yutechhn.cn/badminton/)与[API 健康检查](https://api.yutechhn.cn/api/v1/health)已更新。沿用现有腾讯云服务器，不迁移托管平台。
- 新增 `20260905180000_coupon_booking_scope` 迁移，线上累计33个迁移完成；既有商户券模板 `allowVenueBooking` 全部为 false。
- 未重新 seed 数据，没有重置会员、订单、资产、已付款记录或上传文件；也未执行真实支付、退款或微信发布。
- 小程序本体仍须用本次配套构建单独上传。H5 更新不等于微信客户端包更新。

## 发布验证

1. 先备份当前数据库，恢复至仅本机可访问的临时副本，迁移成功后运行9组集成检查：生产模式拒绝开发登录、员工目录权限与最小字段、冻结余额、跨会员拒绝、充值渠道、五类未付款订单幂等取消与资源释放、超时审计、旧券订场阻断、报价变化回滚。
2. 演练发现两处测试脚本与实际契约不一致，已修正并重跑全绿：来源枚举是 `MINI_PROGRAM`；支付预检对不可用渠道返回200及禁用原因，实际违规付款返回409。没有为迎合断言放宽业务校验。
3. 切换时短暂停止 API，重新完整备份；对74张业务表核对迁移前后的行数及排序内容摘要，全部一致。只排除本次新增券范围字段和迁移元数据；未改写既有业务字段。
4. API 启动后完成6组线上只读检查：HTTPS/CORS、迁移与券策略、订单筛选及匿名拒绝、员工目录权限、支付预检/券包新契约、H5 入口及带版本 JS/CSS。读取权限使用既有种子测试身份的短期令牌，不登录或修改真实会员。
5. 20:53 服务 `active/running`，自动重启计数0，HTTPS 健康检查成功。
6. 浏览器标签已到达“登录”页面，但当前浏览器自动化读取页面时超时；本次不据此宣称新增线上视觉或真机验收通过。静态入口及资源的 HTTP 核验已通过，之前本地 H5 验收证据见收口记录。

演练数据库 `yanqing_release_smoke_20260905_gwzysp` 已清理，源备份保留，可重新恢复演练。临时验收账号及订单没有进入线上数据库。

## 构建与缓存

H5 使用 remote 数据源、`https://api.yutechhn.cn/api/v1` 和 `/badminton/` 资源前缀构建。本地5184模拟数据预览未被替换。

API归档 SHA256：`f2ccbe0ee6cda516e17d479c1b428dad6809fe1247af1ace55072ac9d464f6d0`。

H5归档 SHA256：`396110b652aff9bfa8223132392908441f3aedb09fd624b39f797a07d4151985`。

API归档应用产物来自 b3873cd；其后补充的验收脚本来自 b0e56f6，不改变运行时代码。新 H5 中保留上一版本未冲突的带哈希静态资源，供已打开页面延迟加载；入口引用的是本轮新构建。

## 服务器版本与备份

以下为远端服务器路径，不是开发机路径：

- 当前 API：`/home/ubuntu/yanqing-domain/releases/api-20260905-b3873cd`。
- 当前 H5：`/var/www/yanqing-badminton/releases/h5-20260905-b3873cd`，由 `current` 符号链接指向。
- 前一 API：`/home/ubuntu/yanqing-domain/releases/api-20260905-1615`。
- 前一 H5：`/var/www/yanqing-badminton/releases/h5-align-202609042327`。
- 备份目录：`/home/ubuntu/yanqing-domain/backups/api-20260905-b3873cd`，仅部署用户可访问。含两份数据库备份、原 systemd 覆盖配置、迁移与演练日志、74张表迁移前后摘要、切换脚本。
- API 仍使用原独立环境文件和上传存储目录，未复制密钥至 Git 或 H5。

切换脚本设有失败回退应用/H5的保护，本次未触发。若后续要回退，先判断是否有新业务写入；只能按需回退应用版本，不得用旧数据库覆盖新业务。旧 API 不具备新券规则，回退旧代码会恢复旧行为，须单独评估，不能视为无业务影响。

## 仍需用户确认的安全项

服务器原配置仍是 staging，允许开发身份登录，同时已经配置真实微信支付。本次已询问是否关闭开发登录，尚未获得回复，因此保留既有策略，未自行修改身份权限或强制全员退出。

这不是正式营业的安全验收通过。建议确认后关闭开发登录，再按微信登录、支付唤起/回调、分享卡片、扫码、弱网和实际设备尺寸完成真机复核。隔离副本中“production 拒绝开发登录”的测试不表示当前线上已切到 production。
