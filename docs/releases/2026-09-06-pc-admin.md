# 独立 PC 管理后台 V1 发布记录

日期：2026-09-06（北京时间）。PC 静态页面和 API 已部署，数据库共 34 个迁移。小程序新增扫码授权页已构建，但微信开发者工具持续停留在“准备中”，本次尚未取得上传成功回执，亦未完成真实微信扫码确认。

## 使用入口与范围

电脑打开 <https://api.yutechhn.cn/admin/>。使用新版小程序微信登录后，进入「经营工作台 → 电脑后台登录」，扫描电脑二维码，核对识别码并确认。账号必须具有 ADMIN、SUPER_ADMIN 或 FINANCE 岗位；用户指定的真实微信账号 TY 已在此前授予 SUPER_ADMIN。旧小程序尚无此扫码入口，因此需要先上传新版，并通过预览/体验版完成真机验证；正式版是否提交审核和发布须以微信平台实际状态为准。

| 模块 | V1 能力 |
| --- | --- |
| 我的工作 | 当前岗位待办、到期提醒、业务分组及详情 |
| 人员与权限 | 昵称/手机号、岗位、账号状态筛选；超级管理员修改岗位、商户关联和停用状态，要求变更依据 |
| 业务配置 | 会员产品、充值方案、场地价格、培训产品、联盟券模板；按原 API 支持新建/版本变更、启停和适用范围 |
| 订单查询 | 状态筛选、分页、订单详情 |
| 操作审计 | 对象筛选、分页、变更前后记录，按本人服务端权限读取 |
| 登录设备 | 查看有效电脑会话，电脑和小程序均可撤销，支持退出登录 |

待办的具体履约、退款与结算复核、完整排课和批量作业尚未迁入 PC，继续使用现有小程序工作台。页面使用真实 API 和同一数据库；未创建额外业务账或演示身份。

## 认证与数据

- 两分钟一次性二维码，保存浏览器密钥/扫码密钥哈希；审批必须来自真实微信登录，兑换必须持有原浏览器 Cookie。并发兑换只能成功一次。
- 电脑会话最长 8 小时，使用 `__Host-`、HttpOnly、Secure、SameSite=Strict Cookie；不将登录令牌存入 localStorage。变更请求校验精确 Origin 和 CSRF。
- 每次访问重新核实账号状态和当前角色，停用、撤销岗位、撤销会话立即生效。授权、登录、配置变更留存审计。
- `ADMIN_CONSOLE_ORIGIN=https://api.yutechhn.cn`。不设置时 PC 认证关闭；不允许通配 Origin。
- 新增 AdminLoginChallenge、AdminBrowserSession 两张表，未修改现有业务数据。开发登录仍关闭，支付和原环境文件校验和保持不变。

## 验证记录

- API 全量 98 文件、742 项测试通过；其后新增 10 项 Origin 边界与 1 项非 ASCII CSRF 回归，最终相关 2 文件共 36 项通过。API 重新构建通过。
- API E2E：2 文件、6 项通过。小程序：46 文件、245 项通过。
- 隔离 PostgreSQL + 真实 HTTP：63 项通过，覆盖一次性扫码审批、并发兑换、Cookie/CSRF、会话失效、人员授权，以及五类业务配置创建/启停/跨端读取。
- PC 类型检查与生产构建通过。小程序正式 remote 构建及包体检查通过，主包 667,529 字节，总包 1,169,472 字节，无未使用主包 JS、mock 依赖或超限媒体。
- 服务器完整数据库备份 410,732 字节；恢复到独立临时数据库成功，34 个迁移应用成功，Schema diff 为空，临时数据库随后删除。
- 线上 21 项 HTTP 检查通过：PC 与资源 200、API 健康、匿名/开发身份拒绝、短时二维码创建和取消、安全 Cookie 属性、旧 H5 410、主站 200。线上静态 JS/CSS SHA-256 与本地一致。
- `git diff --check` 通过；API lint 无错误，已有 orders.journey-closeout.spec.ts 未使用导入警告仍存在。
- 尚未执行浏览器视觉/交互验收，尚未取得小程序上传成功回执，真实微信扫码验收待完成。没有伪造线上管理员令牌代替微信确认。

## 部署与回滚

实际服务器：42.193.229.164。

- API systemd：`yanqing-api`；WorkingDirectory `/home/ubuntu/yanqing-domain/releases/api-20260906-pc-v1.1`。V1.1 补齐异常编码 CSRF 输入的拒绝行为。
- PC：`/var/www/yanqing-admin/releases/pc-20260906-v1`；`/var/www/yanqing-admin/current` 指向该目录。
- Nginx：现有 `api.yutechhn.cn` HTTPS server 包含 `/etc/nginx/snippets/yanqing-admin.conf`。模板见 `deploy/nginx-admin-console.conf`。
- 备份：`/home/ubuntu/yanqing-domain/backups/pc-v1-20260906`，包含数据库 dump、恢复/迁移预演日志、原 systemd 覆盖文件、原 Nginx 配置和环境校验和。
- 原 API：`/home/ubuntu/yanqing-domain/releases/api-20260905-auth-closeout`。首次切换时 Nginx reload 后立即探测命中旧 worker 404，自动回滚成功；改用有限重试后发布通过。

需要回退时，将备份 `90-release.conf.before` 恢复到 `/etc/systemd/system/yanqing-api.service.d/90-release.conf`，将 `nginx.before` 恢复到 `/etc/nginx/sites-available/yanqing-api`，执行 systemd daemon-reload、重启 yanqing-api、nginx -t 和 reload。新增表可保留，不直接覆盖当前业务库；数据库恢复只在确认数据影响后单独执行。

## 本地续接

源码位于 `apps/admin`、`apps/api/src/admin-auth` 和 `apps/miniapp/src/packages/ops/pages/pc-login`。开发命令 `ADMIN_DEV_API=http://127.0.0.1:3200 pnpm dev:admin`，API 配置本地精确 Origin `http://127.0.0.1:5190`。生产命令 `pnpm build:admin`。

小程序 AppID 为 `wx25610460bc96894b`，构建目录为 `apps/miniapp/dist/build/mp-weixin`，待上传版本 `0.2.0`。已另存可导入的构建 ZIP；开发者工具恢复后重新执行 upload，获得成功回执后再记录体验版/真机确认结果。此次没有提交 Git commit 或推送远端。
