# 2026-09-05 取消订单接口发布记录

## 发布结果

北京时间 2026-09-05 14:16:59 切换 API，14:17 完成健康检查和路由核对。
公网入口仍为 `https://api.yutechhn.cn/api/v1`。本轮只更新服务器 API；小程序由用户上传，H5 静态资源未更新，未推送 GitHub。

原故障是线上旧版没有 `POST /orders/:id/cancel`，前端调用返回 404。本次部署当前工作区已通过测试的 API 构建，包含：

- 会员取消本人待支付订场、球局订单；他人操作返回 403，已支付订单不能直接取消。
- 订场支付保留期为 10 分钟，后台每 30 秒扫描一次；支付处理中先经微信关单，再取消本地订单并释放场地，失败时保留订单等待处理。
- 球局公开详情、登录后报名名单及本人报名信息接口。
- 兼容旧版“场地已取消、订单仍待支付”的遗留记录：纳入超时扫描，仍执行支付保护、微信关单和事务审计。

两笔旧订场订单在 14:17:03 由自动任务转为 CANCELLED，各有一条 `VENUE_ORDER_AUTO_CANCELLED` 审计；其中一笔原有 WECHAT / PROCESSING 支付记录，已关单并变为 CLOSED。没有手工修改这些订单，也没有发起扣款或退款。

## 验证证据

- 后端单元测试：93 个文件、656 项通过；API 构建通过；健康接口 e2e 1 项通过。
- 本地与线上 Prisma schema 的 SHA-256 一致，30 个已完成迁移与本地目录及 SQL 校验和逐项一致。本轮不执行迁移、不运行 seed。
- PostgreSQL 逻辑备份成功恢复到专用隔离库；隔离 API 只监听 `127.0.0.1:33201`，采用独立测试 JWT、mock 支付，并删除其进程环境中的微信凭据。
- 隔离库真实 HTTP + PostgreSQL 验证 13 项通过：健康、普通会员下单、重复占场阻断、他人取消阻断、本人取消及释放、重复取消幂等与单条业务审计、重新订场、无页面触发的定时超时释放、已支付保护、公共球局详情、名单登录保护、球局报名取消、旧状态自动收尾。
- 自动超时测试仅在隔离库缩短测试订单的截止时间；不声称在真机等待了完整 10 分钟。微信登录与手机端交互仍由用户更新小程序后复测。
- 公网 HTTPS：健康 200；取消接口无凭据返回 401 / 缺少登录凭证，不再是缺失路由 404；名单接口同样要求登录；既有球局详情 200。
- 上线前后：用户 11、订单 16、订单实付汇总 264500 分、退款汇总 0、账户 55、余额汇总 44700 分，均一致。原 API 环境文件逐字节比较一致。
- 临时隔离 API 已停止、专用测试库已删除，33201 不再监听。测试库可以由保留的逻辑备份重新恢复；线上库未删除或重置。

## 运维定位与回退

服务器为 `42.193.229.164`，systemd 服务为 `yanqing-api`。

- 当前工作目录：`/home/ubuntu/yanqing-domain/releases/api-20260905-1356`。
- systemd 覆盖文件：`/etc/systemd/system/yanqing-api.service.d/90-release.conf`，仅覆盖 WorkingDirectory。
- 旧工作目录完整保留：`/home/ubuntu/yanqing-domain/source`。不要误以为向这个旧目录上传代码就会更新现有服务；后续发布先读取 `systemctl show yanqing-api -p WorkingDirectory`。
- 原配置、支付证书和上传文件路径未更改。运行依赖复用旧目录的 pnpm 存储；release 内有独立的 API 依赖链接及 shared 构建，并补齐已安装的 multer 2.2.0 直接依赖链接。不要删除旧 source/node_modules。
- 实际依赖锁基线保存为 release 内 `runtime-pnpm-lock.yaml`，未执行线上全量依赖升级；所有声明的运行依赖均已解析验证。
- 备份：`/home/ubuntu/yanqing-domain/backups/api-20260905-1356`，含 `database.dump`、`api-before.tar.gz`、环境文件副本及原服务定义，目录仅运维用户可读。备份含敏感信息，不得提交 Git。
- 最终包：release 内 `api-release-final.tar.gz`，SHA-256 为 `4e2d983510b7c54b25c88f0dd9e1800ed2cc2ccb8497eed5899a3ddb190b2ff6`。
- 隔离回放记录：release 内 `isolated-smoke-final.log`；测试程序位于其 `apps/api/scripts`，只能恢复指定隔离库后运行，不能改成线上数据库地址。

如需回退，先将上述 `90-release.conf` 移入备份目录，再执行 `systemctl daemon-reload` 和 `systemctl restart yanqing-api`，复核健康接口。无需为代码回退恢复数据库；已经完成的合法取消和审计应保留。

## 尚未覆盖的边界

- 球局自动超时取消和候补主动退出不在本次完成项内；订场自动超时与球局主动取消应分别理解。
- 微信真机新版上传、分享冷启动、本人新订单主动取消仍需在更新后实测；不能用隔离接口测试替代这些体验验收。
- 原配置仍为 `NODE_ENV=staging`、`PAYMENT_PROVIDER=wechat`。本轮按原配置保留，但代码中的开发登录仅在 production 模式关闭；真实经营上线前必须关闭或严格限制开发登录，不能把当前更新视为完成了生产安全验收。
