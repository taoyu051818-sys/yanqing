# 2026-09-07 老板统一摘要与关键事件

PC 后台新增默认入口「老板摘要」，聚合当天订场、代订、实收、履约收入、待支付、取消退款、场地使用和活动报名。后端每5分钟识别并保存关键事件，复用已有风险治理处理与审计流程。

按指定 LLM 服务接入 gpt-5.6-sol，经营事实先由数据库计算，模型只负责文字解读。超时或不可用时返回可直接使用的数据摘要。接口与口径见 [老板摘要接口](../boss-summary-api.md)。

## 验证

- 全量后端：100个测试文件，771项测试通过；最终模型输入调整后相关13项再次通过。
- 独立 PostgreSQL：35个迁移和 seed 成功。汇总计数、代订、实收、确认收入、部分退款跨确认时点、同订单退款去重、事件持久化/幂等/已处理状态保留均通过。
- PC：类型检查与构建通过；真实隔离后端页面加载、模型简报、查看证据、填写说明、标记已处理、显示已处理通过，截图检查首屏与事件列表无布局遮挡。
- lint 无错误，有一处既有订单测试的未使用导入警告。
- 本次未重新验收真实微信扣款/退款和真机分享发布，不据此宣称整个运营闭环已全部验收。

## 发布

- API：`/home/ubuntu/yanqing-domain/releases/api-20260907-boss`。
- PC：`/var/www/yanqing-admin/releases/pc-20260907-boss`。
- 备份：`/home/ubuntu/yanqing-domain/backups/boss-20260907-140928`，包含发布前数据库备份、API配置及PC旧目标。
- 新增迁移 `20260907090000_boss_briefing_events`，只新增结构，无数据清理。
- LLM 密钥为独立服务器环境文件，权限600；原 `.env.api` 校验保持一致。
- 已验证生产健康200、摘要匿名请求401、生产事件扫描成功。
- 模型已在本机及隔离PC后端成功返回 AI 简报；生产服务器连接供应商443端口持续超时（Node connect timeout、curl timeout），两端DNS解析一致。线上当前使用数据摘要回退，需要供应商提供可访问线路或确认服务器IP白名单。此项尚未完成线上AI验收。

## 回滚

恢复备份中的 `90-release.conf` 与原PC目标，移除本次 `95-boss.conf` 环境文件挂载后重新加载 systemd 并重启 API。保留新增事件和简报表，可与旧版兼容；不要为了应用回滚删除经营数据。
