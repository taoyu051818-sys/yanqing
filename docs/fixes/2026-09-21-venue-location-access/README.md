# 球馆地图选点修复（2026-09-21）

## 根因与平台状态

真机点击“地图选点”只收到泛化错误。已在金羽会员（wx25610460bc96894b）的微信后台确认：开发管理 → 接口设置中的 `wx.chooseLocation` 尚未开通。app.json 的 requiredPrivateInfos 和 scope.userLocation 声明已存在，但不能替代平台开通权限。

2026-09-21 09:17 已提交接口申请，附管理员设置球馆位置的真实场景说明和页面截图，后台显示“审核中”。这不是权限已开通的证明；通过前不能宣称真机地图已恢复。

## 修复

- 仅主动点击地图选点时检查隐私授权；需要时使用微信原生同意按钮，确认授权后才打开地图。
- 弹窗可以关闭或拒绝，保留文字地址，不循环弹窗。
- 将用户拒绝、系统定位权限、隐私声明、平台接口权限和普通错误分开提示。用户拒绝后提供主动打开授权设置的入口。
- 保留切换账号、权限收回、退出页面和重置表单的过期回调保护，防止异步结果写入另一份草稿。
- 更新小程序隐私说明，说明只保存确认的球馆位置，用于到馆导航。

## 验证

- 小程序全量：85 个测试文件、540 项测试通过。
- TypeScript / Vue 类型检查通过。
- 微信生产构建通过，产物检查无问题；编译后授权按钮事件、主动调用和地址/坐标回填检查通过。
- 授权流程新增 10 项回归测试，覆盖拒绝、同意后的再次确认、账号与权限变化、页面退出、设置恢复和平台权限错误。

## 真机验收

接口审核通过后，使用更新的体验版：经营台 → 球馆信息 → 修改球馆信息 → 地图选点。首次按需授权；选择球馆位置，确认地址回填，再保存。拒绝授权时应保留文字地址，并可从“打开授权设置”恢复。普通浏览不会请求定位。

本次不涉及服务器 API 或数据库变更。地图能否实际打开仍需微信接口审核通过后的真机验收。

## 09:39 平台更新

微信接口申请已通过，开发管理 → 接口设置中 `wx.chooseLocation` 显示“已开通”。隐私指引已成功补充“选择的位置信息”；体验版已更新至 1.0.8。前文“审核中”是申请提交时的历史状态。接下来可直接进行真机选点验收。
