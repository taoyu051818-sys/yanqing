# 新功能审查问题修复（2026-09-19）

## 修复

1. 微信地图选点：平台判断使用构建工具支持的 process.env.UNI_PLATFORM。新增 scripts/check-wechat-location.mjs，直接执行微信生产包模块、不注入平台变量，验证初次进入不调用地图、点击后调用原生API一次、选点结果回填地址和坐标。检查已接入 build:mp-weixin 与 check:mp-weixin。
2. 培训排课：创建课次事务内按ID排序，对所有场地获取共享行锁并验证存在且未删除；共享锁持续到占场提交，与删除的 FOR UPDATE 互斥。沿用 Serializable 隔离，通过公共 stateTransition 将并发变化转成409可重试提示。旧课次和创建请求的幂等重放保留。

## 验证结果

- API定向单元测试13项、小程序选点测试9项通过。
- 独立本地PostgreSQL的4项集成测试通过：删除/不存在场地拒绝排课且无部分写入；已有课次及幂等重放保留；删除先锁定时排课等待并拒绝；排课先锁定时删除等待并保留已创建记录。
- API构建与lint、小程序类型检查、微信生产包检查通过。
- 微信生产产物调用验证通过，不再提示“请在微信小程序中使用地图选点”。
- 本地H5文字地址保存、会员端展示、无坐标时隐藏导航、地图降级说明和320px布局验收通过。

未部署或上传小程序，未操作生产数据。原生地图与微信授权仍需真机验收；公众平台位置接口权限及隐私声明仍需在发布前确认。没有新增迁移；上一轮 Court.deletedAt 迁移依然是部署这些改动的前提。
