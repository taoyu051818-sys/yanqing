# 小程序修复结果（2026-09-12）

本轮负责的 15 个前端问题已实现并验证代码回归；MINI-004 继续保留待集成验证，不计修复闭环。另配合补齐 VENUE-001 的部分退款签到界面和 PAY-001 的退款结果文案。保留原有球局需求的未提交修改；没有提交、部署、修改线上数据或操作真实支付。PC-001 由 training agent 另行交付。

| ID | 结果与验证 |
| --- | --- |
| MINI-001 | 社区私有状态在会话切换时清空；旧账号请求不再回填。实际 SFC 脚本中验证注销 A → B 登录失败时不残留 A 数据。 |
| MINI-002 | 社区加载增加代次和会话检查，首次列表与后续刷新乱序响应均不能覆盖较新结果或结束较新 loading。SFC 异步回归通过。 |
| MINI-003 | 订场提交回包只有页面仍可见且会话一致才提示或跳转；离页仍保留服务器已创建订单，供正常订单查询恢复。SFC 隐藏页后回包测试通过。 |
| MEVENT-001 | 推荐分享改用独立 referralInvite 参数；兼容原推荐邀请，同时排除赛事邀请路径和 EP 码。归因服务回归覆盖冷/热入口及编码 scene，原推荐不被赛事邀请替换。 |
| MOPS-001 | 已结业学员仍显示有考勤记录的历史课次，可进入消课冲正；不重新加入未来课次候选。实际 attendance composable 回归通过。 |
| MOPS-002 | LEAVE/MAKEUP_REQUIRED 提供安排补课弹窗，只列同班、更晚、SCHEDULED 且目标有 PENDING/LEAVE 考勤的课次；无合法目标给明确提示。接口 payload 与角色检查回归通过。 |
| MOPS-003 | 前台场地订单、退款队列、会员目录支持服务器状态/关键词查询与分页；列表明确总数与已加载数。新查询使旧分页失效，编辑关键词后直接加载更多也从第一页重查。前台和试听统一使用可搜索分页的会员弹窗，取消首 20 人的隐含默认选择。实际 SFC 队列查询和通用分页回归包含第 21 条及乱序响应。退款/会员待办目标不在首屏时可按其授权详情接口定位。 |
| MOPS-004 | 前台核销进入商户工作区，使用当前商户选择与核销确认；移除首商户直接核销及无用首屏商户加载。SFC 验证导航目标，不触发硬编码商户变更。 |
| MOPS-005 | 客户 360 立即清旧数据，按会员/操作人/请求代次接收响应；账户调整确认和提交使用同一已加载会员快照。实际 SFC 的 A/B 乱序响应、加载期间禁操作及 B 账户 payload 回归通过。 |
| MOPS-006 | 待办根据角色和业务类型选择合法页面；商户结算到 merchant 并定位对应商户账单，球局/赛事订单携带 gameId/eventId/报名对象 ID，分组入口同样按角色选择。14 条深链与 5 条工作组回归覆盖所有分组角色，以及越权目标拒绝。后端已同步补齐域 ID 元数据。 |
| MOPS-007 | 空白数字不再转为 0；明确填写 0 保留。保存前弹窗展示解析后的值、生效时刻及未来订单快照说明。18 条参数回归通过。 |
| MOPS-008 | 正常赛事签到交由服务端可配置时间窗校验；管理员历史补录作为独立入口并填写理由。实际 composable 覆盖默认 30 分钟外、服务器可配置 60 分钟内的请求不被前端提前拦截。 |
| INV-003 | 编辑库存资料仅发送变化字段；日期未改时不重写原精确效期。商品有库存时改名称/价格的实际模块测试及独立契约复核通过。 |
| INV-004 | 调拨/报损选择实际来源库位库存分账，展示批次、效期、数量，按该分账余量校验并发送原始 ISO；采购允许核对实际批次/日期，既有目标库位分账保留精确 ISO 或 null，新批次才按所填日期转日末。操作过账丢失回包使用稳定重试键。4 条库存回归及 training agent 独立完整模块复核通过（含不同库位、午夜/非日末时刻、无效期与丢包重试）。 |
| ALLIANCE-001 | 财务 DRAFT 卡片可打开“更正草稿”弹窗，填写毛利与原因，使用稳定重试键调用 revise，显示前后版本与理由，再提交商户确认。非 DRAFT 不开入口；丢包重试同键回归通过。经营摘要标签明确当期结算毛利/费用/ROI。 |
| VENUE-001（联动） | 部分退款且净实付大于 0、预约为 CONFIRMED 时显示签到；已签到或全额退回不再显示签到。待签到统计与按钮同口径，默认全部场地单，不伪改订单财务状态。读取原 SFC 按钮模板并通过 Vue.compile 实际渲染三种情形，全部通过。 |
| PAY-001（联动） | 审批返回 SUCCEEDED 显示已完成；APPROVED/PROCESSING 显示等待支付方同步。原按钮仅 REQUESTED/REFUND_PENDING 可批/驳，未扩大权限。三种返回状态回归通过。 |

MINI-004：原函数隔离重放证实确认目标使用可变路由 ID，但本轮没有足够的“有效球局 A 弹窗 → 实际路由切换 B → 确认提交”完整浏览器证据。未改该报名逻辑，也未把既有工作区 game-detail 修改算作本轮修复。微信原生同页复用行为仍未验证。

## 验证记录

- 小程序全套：**69 文件、398 测试通过**，26.29 秒，见 `evidence/miniapp-tests.log`。最后测试类型标注仅精确描述 Vue.compile 渲染函数，不改变测试运行逻辑。
- `vue-tsc --noEmit`：**通过**，见 `evidence/miniapp-typecheck.log`。
- 微信 remote 构建与包门禁：**通过**，见 `evidence/miniapp-mp.log`。主包无孤立 JS、无 mock 文件、无超限媒体。第一次发现仅经营子包使用的分页 helper 出现在主包，已迁入 `packages/ops/utils` 并验证通过。
- H5 remote 构建：**通过**，见 `evidence/miniapp-h5.log`。输出 `/tmp/yanqing-miniapp-fixes-20260912-h5`；微信输出 `/tmp/yanqing-miniapp-fixes-20260912-mp`。
- `git diff --check -- apps/miniapp/src`：通过。
- 独立库存契约复核：`evidence/inventory-cross-contracts.cjs` / `.after.json`，由 training agent 运行并包含源码 SHA。

## 界面验收与边界

使用本机隔离 mock 服务和原生 Chrome CUA 打开前台、会员选择弹窗。没有借用服务器账号或产生真实业务订单。桌面 H5 390px 预览画布中的页面、搜索、结果、状态区正常；实际发现原固定会员层受 #app transform 影响，标题/搜索越出视口。改为复用系统 ActionDialog，H5 将弹窗传送到 body，主题变量同步给弹窗；截图复核已看到完整标题、绿色搜索按钮、会员列表、取消栏与绿色/金色边线，恢复原视觉风格。截图保留在本次工具记录中；原生 CUA 没有文件保存接口，未虚构 PNG 附件。

最后一项 H5 调整是修正弹窗高度测量：传送后必须按实际 dialogId 在 body 中测量，原 uni 组件选择器仍在页面内；仅 H5 使用 DOM 测量，微信继续 uni selector。该最终测量改动已通过 H5 构建和 type-check，但未再次进行完整浏览器尺寸矩阵操作；微信键盘、375px/横屏和真机行为仍需人工验收。用户在同一 Chrome 继续使用其他标签时，工具提示窗口已变化，因此停止进一步操作，没有干扰用户页面。

自建预览仅监听 `127.0.0.1:5201`，已按核对过的本仓库进程 PID 51603 发出 SIGTERM 并清理。其余现有服务未操作。无真实微信登录、支付、分享或扫码验收结论。
